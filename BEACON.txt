BEACON - concept for an ideal blogging platform
===

site
	.title
	.desc
	.home
	.theme
	.plugins
	.apps
	.fields   	<< allow custom fields on the site	
	
page

	.title (in the head)
	.template
	.type (category / single / page / archive / etc)
	.slug
	.permalink
	.posts
	.bodyclass

post
	.id
	.title
	.slug
	.permalink
	.contentType
	.content({ limit: 100; images: false; html: false; link: "Read More..."})
	.date
	.modified
	.status
	.template
	.cats
	.tags
	.taxonomies
	.fields
	.comments
	.author	
	.parent
	.order
	.commentCount
	
image
	.id
	.slug
	.ext
	.path
	
cats
tags
	
users			// this should really just be a contentType
	.id
	.username
	.realname
	.access
	.avatar
	.links
	.desc
	.fields
